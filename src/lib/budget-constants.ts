// Plain (non "use server") shared constant: server action files can only
// export async functions, so this can't live alongside the budget actions.
export const UNEXPECTED_CATEGORY_NAME = "Unexpected expenses";
